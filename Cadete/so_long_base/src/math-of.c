#include <math-of.h>

t_vi2d   vi2d_start(B32 x, B32 y)
{
	t_vi2d	set;

	set.x = x;
	set.y = y;
	return (set);
}

t_vf2d   vf2d_start(BP32 x, BP32 y)
{
	t_vf2d	set;

	set.x = x;
	set.y = y;
	return (set);
}